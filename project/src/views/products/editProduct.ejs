<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

	<body>
		<%- include('../partials/header') %>
			<main class="container">
				<div class="product-form">
					<h1>Editar producto</h1>
					<form action="/products/edit/<%= product.product_id %>?_method=PUT" method="POST"
						enctype="multipart/form-data">
						<input class="product-form__image" type="file" placeholder="Imagen" name="image" required>
						<input type="text" placeholder="Nombre del producto" name="name" value="<%= product.name %>" required>
						<input type="number" placeholder="Precio" name="price" value="<%= product.price %>" required>
						<input type="number" placeholder="Descuento" name="discount" value="<%= product.discount %>" required>
						<input type="text" placeholder="Título de la descripción" name="descriptionTitle" value="<%= product.description_title %>" required>
						<textarea placeholder="Descripción" name="description" required><%= product.description %></textarea>
						<input type="number" placeholder="Stock" name="stock" value="<%= product.stock %>" required>
						<textarea placeholder="Características del producto" name="specs" required><%= product.specs %></textarea>
						<select class="controlar" name="category" id="category">
							<option value="">Elegí una categoría:</option>
							<% for(const oneCategory of productsCategories) { %>
								<% if (oneCategory.category_id === product.category_id) { %>
									<option value="<%= oneCategory.name %>" selected><%= oneCategory.name %></option>
								<% } %>
								<option value="<%= oneCategory.name %>"><%= oneCategory.name %></option>
							<% } %>
						</select>
						<select class="controlar" name="brand" id="brand">
							<option value="">Elegí una marca:</option>
							<% for(const oneBrand of productsBrands) { %>
								<% if (oneBrand.brand_id === product.brand_id) { %>
									<option value="<%= oneBrand.name %>" selected><%= oneBrand.name %></option>
								<% } %>
								<option value="<%= oneBrand.name %>"><%= oneBrand.name %></option>
							<% } %>
						</select>
						<input type="text" placeholder="Color (opcional)" name="color">
						<button type="submit" class="product-form__button">Confirmar</button>
					</form>
				</div>
			</main>
			<%- include('../partials/footer') %>
	</body>

</html>